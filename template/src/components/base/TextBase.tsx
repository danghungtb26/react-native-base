import React from 'react'
import { Text, View } from 'react-native'

interface IProps {}

const Index: (props: IProps) => React.ReactNode = () => {
  return (
    <View>
      <Text>Index</Text>
    </View>
  )
}

export default Index
